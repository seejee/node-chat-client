#!/usr/bin/env node

var log = require('verbalize');
log.runner = 'chat-client';

var argv = require('minimist')(process.argv.slice(2));
var url = argv.url || 'http://localhost:3000/faye';

require('../src/load_test')({url: url});
